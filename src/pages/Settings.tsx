/**
 * Settings
 *
 * Placeholder for the settings page.
 * Will hold the dark mode toggle and future user preferences.
 * Will be filled in TASK-019.
 *
 * @returns {JSX.Element} Placeholder settings page.
 */
const Settings = () => {
  return (
    <div className="flex h-full items-center justify-center text-muted-foreground">
      <p>Settings — coming soon (TASK-019)</p>
    </div>
  );
};

export default Settings;
