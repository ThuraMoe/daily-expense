/**
 * Login
 *
 * Placeholder for the login page with Google sign-in.
 * Will display a Google sign-in button using Firebase auth popup flow.
 * Will be filled in TASK-004.
 *
 * @returns {JSX.Element} Placeholder login page.
 */
const Login = () => {
  return (
    <div className="flex min-h-screen items-center justify-center text-muted-foreground">
      <p>Login — coming soon (TASK-004)</p>
    </div>
  );
};

export default Login;
