/**
 * Dashboard
 *
 * Placeholder for the main dashboard overview.
 * Displays a summary of recent spending, category breakdowns, and quick actions.
 * Will be filled in TASK-017.
 *
 * @returns {JSX.Element} Placeholder dashboard page.
 */
const Dashboard = () => {
  return (
    <div className="flex h-full items-center justify-center text-muted-foreground">
      <p>Dashboard — coming soon (TASK-017)</p>
    </div>
  );
};

export default Dashboard;
