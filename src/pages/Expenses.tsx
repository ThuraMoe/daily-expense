/**
 * Expenses
 *
 * Placeholder for the expense list page.
 * Will display daily/monthly/yearly views, filters, and CRUD operations for expenses.
 * Will be filled in TASK-007 through TASK-013.
 *
 * @returns {JSX.Element} Placeholder expense log page.
 */
const Expenses = () => {
  return (
    <div className="flex h-full items-center justify-center text-muted-foreground">
      <p>Expenses — coming soon (TASK-007)</p>
    </div>
  );
};

export default Expenses;
